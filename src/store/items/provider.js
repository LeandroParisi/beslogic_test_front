import BaseProvider from '../BaseClasses/BaseProvider'

class ItemProvider extends BaseProvider {
  // Not extended yet
}

const ItemsProvider = new ItemProvider('items')

export default ItemsProvider
